<h1>Taxas</h1>
<br />

<p>
    <a href="<?php echo $this->url('livraria-admin',array('controller'=>'taxas','action'=>'new')); ?>"><i class="icon-plus"></i>Nova Taxa</a>
</p>
<?php
$coluns = array('ID','Seguradora','Classe','Inicio','Fim','Incêndio','Incêndio<br>Conteúdo','Aluguel','Eletrico','Desastres','Status','Ação');
$td     = array('','','','','class="r"','class="r"','class="r"','class="r"','class="r"','','');
echo $this->ViewIndex('table'),
     $this->ViewIndex('thead', array('coluns' => $coluns, 'tdopt' => $td,'editLine' => 'last'));
     
foreach($this->data as $entity){ 
    $linha = array(
        $entity->getId(),
        $entity->getSeguradora(),
        $entity->getClasse(),
        $entity->getInicio(),
        $entity->getFim(),
        $entity->floatToStr('incendio'),
        $entity->floatToStr('IncendioConteudo'),
        $entity->floatToStr('Aluguel'),
        $entity->floatToStr('Eletrico'),
        $entity->floatToStr('Desastres'),
        $entity->getStatus(),
        $entity->getId(),
    );
    echo $this->ViewIndex('line',array('data' => $linha));
}
echo $this->ViewIndex('close');
?>
<br />
<form name="form" id="form" method="POST">
    <input type="hidden" name="subOpcao" id="subOpcao2">
    <input type="hidden" name="id" id="id2">
</form>
<script language="javascript">
    function edit(id){
        try{
            var vlr = document.getElementById('seguradora').value;
            var ele = document.createElement("input");
            var frm = document.getElementById('form');
            ele.setAttribute("type","hidden");
            ele.setAttribute("name","seguradora");
            ele.setAttribute("id","seguradora2");
            ele.setAttribute("value",vlr);
            frm.appendChild(ele);    
        }catch(e){
            var vlr = e ;
        }        
        document.getElementById('id2').value = id ;
        var tar = "<?php echo $this->url($this->matchedRouteName,array('controller'=> $this->params['controller'],'action'=>'edit')); ?>";
        envia(tar,'busca');
    }    
    function del(id){
        var msg = "Deseja realmente excluir este registro?";
        if(!confirm(msg))return;
        var tar = "<?php echo $this->url($this->matchedRouteName,array('controller'=> $this->params['controller'],'action'=>'delete','id'=>'')); ?>";
        envia(tar + id);        
    }
</script>

<?php echo $this->paginationControl($this->data,'Sliding','paginator',array('route' => $this->route)); ?>